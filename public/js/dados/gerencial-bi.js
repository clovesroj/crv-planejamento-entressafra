/**
 * GERADO por scripts/gerencial-bi.mjs -- ainda nao rodado pra valer (rode
 * scripts/rodar-gerencial-bi.ps1 ou node scripts/gerencial-bi.mjs pra
 * preencher). Este stub so existe pra tela nao quebrar (import estatico em
 * ui/frota.js) enquanto o arquivo de verdade nao foi gerado.
 */
export const GERENCIAL_BI = { geradoEm: null, periodos: [], colunasPorPagina: {}, registros: [] };
